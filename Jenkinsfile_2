def gv
pipeline{
    agent any

    parameters {
    // choice choices: ['main', 'test'], name: 'branch'
    choice choices:["dockerhub", "ECR"], name: "image_repository"
    


    }

stages{

    stage('gitclone'){
        // when {
        //     expression{
        //              params.branch == "test"
        //     }
           
        // }
        steps{
            script{
                if(BRANCH_NAME == "main"){
                    echo "you are not in $BRANCH_NAME branch"
                    gv = load 'script2.groovy'
                    gv.showWorkingDir()
                }
                
            }
        }
    }

    stage('Building Jar '){
        steps{
            script{
                gv.buildJar()
            }
        }
    }

    stage("Build image"){
        steps{
            script{
                gv.buildImage()
            }
        }
    }

    stage("push image to dockerhub"){
        steps{
            input message: 'Do you want to deploy',
            ok: 'OK',
            parameters: [choice(choices: ['DEV', 'Stage', 'Prod'],
            name: 'ENV')]

            script{
                if (params.image_repository == "dockerhub"){
                    gv.push_dockerhub()
                }
                else{
                    sh 'echo "fail"'
                }
            }
        }
    }



}
}